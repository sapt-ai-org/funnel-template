/** Optional extension switch. The live funnel itself is owned by funnel.ts. */
export const siteConfig = {
  // When enabled, `resolveContent` can layer published CMS section records over
  // caller-provided code defaults. The main funnel never enables this implicitly.
  useCmsContent: false,
}
